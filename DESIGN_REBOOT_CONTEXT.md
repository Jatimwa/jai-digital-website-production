# Design System Reboot Context (Pre-v4)

**Saved:** May 6, 2026

## User's Provided Context
"yes i can see the last session did not save, so context. we got the design system v3 looking really good then the conversation compacted and things went down fast, the good design got completely over written, we created an audit of all the versions, with the intention of created v3.1, what was supposed to be saved for this session, the design-system page was created fresh as we had so many issues witht the width constraints, so that page should be a fresh build without any legacy, we then locked in the fonts, sizes and a full dark themne colour palette as we only had 1 oe 2 backgroundand card colours etc. this is as fair as we got nothing else shopuld be in v3.1 so i am not sure why the homepage evern exists. i did say lets start fresh completey but was sured this woudln't cross over and could be done cleanly, clearly not."

## Directives for Audit
- Do not peek, assume, or take anything for granted.
- Perform a full, comprehensive audit of the entire workspace.
- Breakdown:
  - Versioning (what is where).
  - Colors used previously (CSS, hardcoded OKLCH, hex, Tailwind classes).
  - Fonts and sizes used.
  - Sections available and duplicated.
- Goal: Move to a clean v4 without carrying any legacy garbage across.
